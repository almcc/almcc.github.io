# macOS
.DS_Store

# Next.js build cache
.next/

# Dependencies
node_modules/

# Local env files
.env*.local

# Build output (committed intentionally via docs/)
# docs/ is NOT ignored — it is tracked in git for GitHub Pages deployment
